import players from './players'
import teams from './teams'
import matches from './matches'

export default [players, teams, matches]
export const Players = players
export const Teams = teams
export const Matches = matches
